import { IdentityDataInputComponent } from "./identity-data-input.component";

describe('YearMonthInputComponentSuite', () => {

  let component: IdentityDataInputComponent;

  beforeEach(() => {
    component = new IdentityDataInputComponent();
  });

  it('expect true to be truthy', () => {
    expect('bla').toEqual('bla')
  });

});
